# v227 忠実・ゲーム別分離版の運用

この版は、v227のDOM、CSS、ゲーム処理、起動順を保持したまま、CSSとJavaScriptを外部ファイルへ分離した実用版である。`core-v228.js`には共有状態・保存・時間計算、`abyss-v228.js`、`generation-v228.js`、`starleap-v228.js`にはゲーム別処理、`bootstrap-v228.js`には起動・復帰・サービスワーカー登録処理を保持する。CSSは`styles-v228.css`を使用する。

## 安全条件

保存キーは`dotabyss:unified:v1`であり、v227および既存Abyss.2と同じ保存を引き継ぐ。サービスワーカーはAbyss.2専用のキャッシュを使う。

## 公開判断

v227は唯一の完成基準として維持する。Abyss.2へ公開する場合は、既存保存が継続して表示されること、Gジェネのデイリー長押し、スターリープの2回タップ、ドットアビスの放置報酬確認待機を実機で確認する。

> 次の細分化は、ゲーム別の責務を分ける前後で同じ回帰検証を通してから行う。見た目や操作を変えるためのリファクタリングは行わない。